问题 1：PerceptionService 目前 没有被任何 Controller/Service 注入调用 （只有自身和 ToolResultParser 引用它）。当前主链路是：

```
AskController -> QuickAskService -> IntentClassifier（直接分类）-> ReActExecutor
```
也就是说，目前用户输入是纯文本（DTO 只有 String query ），直接进 QuickAskService，没有走完整的 7 步感知管线。

输入类型 HTTP 传输方式 Controller 接收 RawInput 封装 文本 application/json body: {"query":"HashMap原理"} DTO: dto.getQuery() new RawInput("HashMap原理", null, null, null, userId, sessionId, TEXT) PDF multipart/form-data 上传文件 MultipartFile.getBytes() new RawInput(null, fileBytes, "简历.pdf", "application/pdf", userId, sessionId, PDF) 图片 multipart/form-data 上传文件 MultipartFile.getBytes() new RawInput(null, fileBytes, "截图.png", "image/png", userId, sessionId, IMAGE) 工具返回 不经过 HTTP，ReAct 循环内产生 ToolResult 对象 不封装为 RawInput，走 ToolResultParser 独立路径

记忆层的问题：
你的质疑完全正确，这确实是一个**设计缺陷**。

---

## 问题分析

### RRF 融合的前提

RRF 的设计目的是融合**对同一 query 的多路检索结果**，消除不同打分体系的量纲差异。它假设各路检索都是"根据 query 检索相关内容"，只是检索方式不同。

### 四路检索的性质

| 路径 | 是否依赖 query | 排序依据 | 适合 RRF？ |
|------|--------------|---------|-----------|
| 语义检索 | **是**（query 向量相似） | 与 query 的语义相似度 | ✅ |
| 关键词检索 | **是**（query BM25 匹配） | 与 query 的关键词匹配度 | ✅ |
| 时间检索 | **否**（只看 userId） | 时间新近度 | ❌ |
| 薄弱点检索 | **否**（只看 userId） | 掌握度/出现次数 | ❌ |

### 具体问题

**薄弱点检索**：`getWeakPoints(userId)` 返回用户所有薄弱点。用户问"HashMap原理"，但薄弱点列表可能有"Redis持久化"、"TCP握手"——和 query 无关。但 RRF 会给薄弱点列表中排第 1 的一个较高分数（`1/61 = 0.0164`），它可能挤掉真正和 HashMap 相关的语义检索结果。

**时间检索**：`getRecentRecords(userId, 7)` 返回最近 7 天的面试记录。最近面试可能问的是"Spring IOC"，但当前查询是"HashMap原理"。最新的记录获得高 RRF 分数，但和 query 完全无关。

### 你的代码其实已经有 query-dependent 的薄弱点检索了

```java
// 路径1：语义检索 -- 已经是根据 query 找相关的薄弱点了
semanticMemoryService.searchSimilarWeakTopics(userId, query, 10);

// 路径4：薄弱点检索 -- 又全量取了一遍，和 query 无关
semanticMemoryService.getWeakPoints(userId);
```

路径 1 已经做了"根据 query 找语义相似的薄弱点"，路径 4 又全量取所有薄弱点，**冗余且会引入噪声**。

---

## 正确的设计应该是

```
┌──────────────────────────────────────────────────────┐
│  Query-dependent 检索（参与 RRF 融合）                │
│  ├── 语义检索：searchSimilarWeakTopics(userId, query) │
│  └── 关键词检索：knowledgeRetriever.retrieve(query)   │
│                                                       │
│  → RRF 融合 → Top-5 相关记忆                          │
├──────────────────────────────────────────────────────┤
│  Query-independent 注入（直接注入 Context，不参与 RRF）│
│  ├── 薄弱点摘要：getWeakPoints(userId) 的统计信息      │
│  │   "你有 3 个顽固薄弱点：HashMap、并发、GC"          │
│  └── 最近面试记录：getRecentRecords(userId, 7)         │
│      "上次面试：2026-07-28，得分 75，考了 Spring IOC"  │
└──────────────────────────────────────────────────────┘
```

**区别**：
- RRF 融合的结果进入 Context 的"相关记忆"区域（和 query 强相关）
- Query-independent 的信息进入 Context 的"用户画像"区域（个性化背景，不是检索结果）

### 薄弱点应该怎么给

不是全量给所有薄弱点（Token 浪费 + 无关信息干扰），而是给**摘要**：

```
用户画像：
- 顽固薄弱点：HashMap（掌握度 30%）、JVM GC（掌握度 40%）、并发（掌握度 45%）
- 上次面试得分：75/100
- 建议重点考察：HashMap（连续 3 次答错）
```

这样 LLM 知道用户的薄弱点全貌，但不会用 20 条薄弱点详情占满 Token。

---
